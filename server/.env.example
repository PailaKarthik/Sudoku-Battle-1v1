# PostgreSQL connection string used by Prisma and the NestJS application.
# Replace the credentials, host, port, and database name for your environment.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sudoku?schema=public"

# Optional HTTP port for NestJS.
PORT=3000
